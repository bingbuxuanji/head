# 智能头盔 MQTT 服务器依赖
# Python 3.8+ 标准库已涵盖大部分需求（asyncio, struct, logging, json）
# 本文件仅记录少量可选的外部依赖

# 如需与外部 MQTT broker 桥接（可选）:
# paho-mqtt>=2.0.0

# 如需使用 Redis 作为会话/认证存储（可选）:
# redis>=5.0.0

# 生产部署建议:
# - 使用 supervisor 或 systemd 管理服务器进程
# - 使用 nginx 做 TLS 终止（MQTT over TLS 推荐）
